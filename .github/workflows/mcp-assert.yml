name: mcp-assert

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  assert:
    uses: wyre-technology/.github/.github/workflows/mcp-assert.yml@main
    with:
      entry: dist/index.js
      canary-tool: xero_invoices_list
