name: xero_invoices_list returns is_error when credentials missing
server:
  command: node
  args: [dist/index.js]
assert:
  tool: xero_invoices_list
  args: {}
  expect:
    is_error: true
timeout: 30s
