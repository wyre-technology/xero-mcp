{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.wyre-technology/xero-mcp",
  "title": "Xero",
  "description": "MCP server for Xero accounting — invoices, bills, contacts, accounts, and reports.",
  "repository": {
    "url": "https://github.com/wyre-technology/xero-mcp",
    "source": "github"
  },
  "version": "0.0.0",
  "websiteUrl": "https://github.com/wyre-technology/xero-mcp",
  "packages": [
    {
      "registryType": "oci",
      "identifier": "ghcr.io/wyre-technology/xero-mcp:0.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "XERO_CLIENT_ID",
          "description": "Xero OAuth2 app client ID",
          "isRequired": true,
          "format": "string"
        },
        {
          "name": "XERO_CLIENT_SECRET",
          "description": "Xero OAuth2 app client secret",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "XERO_REFRESH_TOKEN",
          "description": "Xero OAuth2 refresh token used to mint access tokens",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "XERO_TENANT_ID",
          "description": "Xero tenant (organisation) ID to scope API calls to",
          "isRequired": true,
          "format": "string"
        },
        {
          "name": "MCP_TRANSPORT",
          "description": "Transport mode for the server. Set to 'stdio' for local CLI use; the image defaults to 'http' for gateway hosting.",
          "isRequired": false,
          "default": "stdio",
          "format": "string"
        },
        {
          "name": "AUTH_MODE",
          "description": "Credential source: 'env' reads vars locally, 'gateway' expects header injection from the WYRE MCP Gateway.",
          "isRequired": false,
          "default": "env",
          "format": "string"
        },
        {
          "name": "LOG_LEVEL",
          "description": "Log verbosity: debug, info, warn, error",
          "isRequired": false,
          "default": "info",
          "format": "string"
        }
      ]
    }
  ]
}
